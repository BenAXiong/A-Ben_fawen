    // const lastTense = tense ?? localStorage.getItem("lastTense") ?? "present";
    // const lastVerbs = verbs ?? JSON.parse(localStorage.getItem("lastVerbs") ?? '["régulier"]');
  
    // localStorage.setItem("lastTense", lastTense);
    // localStorage.setItem("lastVerbs", JSON.stringify(lastVerbs));